# Screen: Страница ресурса (resource)

**Wireframe:** docs/ia/wireframes/resource.md · **Шаблон:** templates/single-resource.html · **URL:** /resource/{slug}/
**Партия:** 1 «Каталог» · **Статус:** verified

## Composition

### header
- `parts/header.html`. Текущий пункт меню — раздел типа ресурса.

### breadcrumbs
- `core/breadcrumbs` с классом `ds-breadcrumbs`: «Главная › Инструменты › Figma». Среднее звено — раздел по типу, название берётся из карты разделов, а не из термина.

### resource-layout (локальная композиция `.ds-detail`)
- `core/group {"className":"ds-detail"}`: от 900 — две колонки, слева 2/3, справа 1/3, зазор `space-8`; до 900 — одна колонка в порядке шапка → факты → оценка → аналоги.
- Ширина `wide` (1360, D41).

### resource-hero (левая колонка)
- block `designstack/resource-hero` собирает первый экран целиком: плитка логотипа `size-tile-lg` → H1 с названием → подпись типа `.ds-card__type` (не ссылка, D35) → вердикт крупнее текста → метки цены, доступа и оплаты (`ds-badges`) → плашка состояния (`ds-notice--dead|changed|stale`) → кнопка «Перейти на сайт» (primary lg, новая вкладка) и строка «Проверено 3 сен 2026».
- H1 внутри блока, а не отдельным `core/post-title`: логотип, название и подпись типа стоят одной строкой, разорвать её отдельным блоком нельзя.
- У закрытого ресурса кнопки перехода нет; если аналог есть — в плашке кнопка «Показать аналог» на `#analogs` (D31).

### resource-facts (правая колонка)
- block `designstack/resource-meta` → `.ds-facts`: пары «подпись — значение», пустое необязательное поле не выводится. Поля своего типа идут после общих.

### curator-review (левая колонка)
- block `designstack/curator-review` → `.ds-review`: H2 «Оценка куратора» и три подписанные части — «Кому подходит», «За что», «Когда не подойдёт» — плюс подпись куратора ссылкой на `/about/#curator`.
- Без «Когда не подойдёт» запись не публикуется (D32), поэтому третья часть на опубликованной странице есть всегда.

### analogs (якорь `#analogs`)
- block `designstack/resource-analogs` → H2 «Аналог из России» и сетка карточек из поля `ru_alternative`. Нет аналога — блока нет.

### footer
- `parts/footer.html`.

## Data

| Блок | Что показывает | Источник |
|---|---|---|
| `designstack/resource-hero` | логотип, название, тип, вердикт, метки, состояние, переход, дата | поля записи |
| `designstack/resource-meta` | факты парами, включая поля своего типа | поля записи и таксономии `level`, `topic` |
| `designstack/curator-review` | три части оценки и подпись куратора | `review_for`, `review_why`, `review_not` |
| `designstack/resource-analogs` | карточки аналогов | поле `ru_alternative` (3 ресурса из 28 заполнены) |

## Edge cases

- Закрытый ресурс — `/resource/invision/`: плашка «Закрыт», кнопки «Перейти на сайт» нет, есть «Показать аналог», блок аналогов ниже.
- Без аналога — `/resource/laws-of-ux/`: блока `analogs` нет, кнопки «Показать аналог» нет.
- Бесплатный ресурс — метки и строки оплаты нет (D28).
- Давно не проверяли — плашка `stale` появляется у записи старше 90 дней; в демо-наборе такие есть.
- Незаполненное необязательное поле — строки в фактах нет.
- Черновик или несуществующий адрес — 404; `/resource/` без слага — 404 (D65).
- Переполнение: название в 80 знаков переносится и не обрезается; длинная `price_note` переносится под значением цены.

### Матрица состояний (этап 14)

| Блок | Состояние | Как закрыто |
|---|---|---|
| Шапка | активен | `/resource/figma/` — вердикт, метки, «Перейти на сайт», дата |
| Шапка | закрыт с аналогом | `/resource/invision/` — плашка «Закрыт», «Показать аналог», кнопки перехода нет |
| Шапка | закрыт без аналога | в плашке «Аналог пока не подобрали» и ссылка «Предложить ресурс» (проверено подменой поля) |
| Шапка | условия изменились | плашка `changed` — Miro в каталоге |
| Шапка | давно не проверяли | плашка `stale` при дате старше 90 дней |
| Шапка | без вердикта | строка не выводится, остальные части карточки и страницы на месте |
| Логотип | картинки нет | первая буква названия на нейтральной плитке |
| Факты | пустое поле | строки нет: подписи без значения не бывает |
| Аналоги | нет аналога | блока нет |
| Страница | снят с публикации | 404; карточка исчезает из раздела, подборки и блока аналогов (проверено на живой записи) |

## Tokens used

surface-default, surface-raised, surface-subtle, text-default, text-muted, text-link, text-on-action, surface-action-primary, border-default, border-strong, border-focus-ring, icon-success, icon-warning, icon-error, bg-warning, bg-error; space-2/3/4/6/8/12; radius-md; shadow-sm; size-tile-lg, size-tile-sm, size-control-lg, size-icon-sm/md, size-measure.

## Icons

circle-check, triangle-alert, circle-x, clock, external-link, chevron-right.

## Локальные композиции

- `ds-detail` — две колонки страницы ресурса (2/3 и 1/3 от 900). Пока встречается один раз; второй кандидат — страница «О проекте» в партии 4, тогда помечаем к выносу.

## Решения состава

- r1. Первый экран собирает один блок, а не набор core-блоков: логотип, название и подпись типа стоят одной строкой, а вердикт, метки, плашка и кнопка перехода зависят от одних и тех же полей — разрезать их на отдельные блоки значит четыре раза читать ту же запись и четыре раза ошибиться в порядке.
- r2. Подпись типа рядом с H1 — не ссылка: метка в каталоге не кликается (D35), а раздел и так стоит в крошках через строку выше.
- r3. Оценка куратора выводится тремя подписанными частями (D67), а не одним текстом: так видно, где ответ на «мне подойдёт?», а где на «когда не брать».
- r4. Блок аналогов показывает те же карточки, что и списки каталога, без облегчённого вида: аналог выбирают так же, как ресурс, — по цене, доступу и дате проверки.
- r5. Подпись куратора остаётся с именем-заглушкой «Мария Петрова» и ссылкой на `/about/#curator`: страница «О проекте» появится в партии 4, имя ждёт решения O2.

## Чеклист страницы

- [ ] На 1440 в первом экране видны логотип, название, вердикт, метки, кнопка перехода и начало колонки фактов.
- [ ] Подпись типа и метки не выглядят кликаемыми, кнопка «Перейти на сайт» — единственная залитая на экране.
- [ ] У закрытого ресурса нет кнопки перехода, а плашка стоит выше места, где она была.
- [ ] Три части оценки подписаны и читаются как разные ответы; текст не шире 70 знаков в строке.
- [ ] Колонка фактов не расширяется от длинного значения; пустых подписей нет.
- [ ] На 375 порядок: шапка → факты → оценка → аналоги; горизонтального скролла нет.
- [ ] Дата проверки читается как дата, а не как ссылка.

Добавлено аудитом этапа 15 (14.09.2026), проверено на живых страницах:
- [x] Карточка аналога идёт уровнем ниже заголовка «Чем заменить».

## Апрув заказчика

Лист решений партии 1 «Каталог»: https://claude.ai/code/artifact/e5ebd45b-228c-44af-bf5b-18de4a8b4c0a (опубликован 12.09.2026).
Ответы даны в чате 12.09.2026 (D47).

| # | Решение | Ответ | Причина |
|---|---|---|---|
| r1 | Первый экран собирает один блок | ✔ | — |
| r2 | Подпись типа рядом с H1 — не ссылка | ✔ | — |
| r3 | Оценка куратора — тремя подписанными частями | ✔ | — |
| r4 | Аналоги — те же карточки, что в каталоге | ✔ | — |
| r5 | Подпись куратора с именем-заглушкой «Мария Петрова» | ✘ | Выдуманного имени на странице быть не должно, пока имя не выбрано (O2). Вместо подписи стоит «Куратор DesignStack» со ссылкой на «О проекте». Разбор по классу: имя-заглушка убрано и из паттерна `curator-review`, проверка — `grep -rn "Мария Петрова" wordpress/wp-content/themes wordpress/wp-content/plugins` пусто (серые шаблоны уходят вместе с прототипом в конце этапа) |

**Статус:** verified — партия принята в чате 12.09.2026.

## Апрув заказчика

Лист решений партии 1 «Каталог»: https://claude.ai/code/artifact/e5ebd45b-228c-44af-bf5b-18de4a8b4c0a (опубликован 12.09.2026).
Ответы даны в чате 12.09.2026 (D47).

| # | Решение | Ответ | Причина |
|---|---|---|---|
| r1 | Первый экран собирает один блок | ✔ | — |
| r2 | Подпись типа рядом с H1 — не ссылка | ✔ | — |
| r3 | Оценка куратора — тремя подписанными частями | ✔ | — |
| r4 | Аналоги — те же карточки, что в каталоге | ✔ | — |
| r5 | Подпись куратора с именем-заглушкой «Мария Петрова» | ✘ | Выдуманного имени на странице быть не должно, пока имя не выбрано (O2). Вместо подписи стоит «Куратор DesignStack» со ссылкой на «О проекте». Разбор по классу: имя-заглушка убрано и из паттерна `curator-review`, проверка — `grep -rn "Мария Петрова" wordpress/wp-content/themes wordpress/wp-content/plugins` пусто (серые шаблоны уходят вместе с прототипом в конце этапа) |

**Статус:** verified — партия принята в чате 12.09.2026.
