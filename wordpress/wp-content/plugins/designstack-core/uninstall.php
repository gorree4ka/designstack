<?php
/**
 * Удаление плагина: чистим только свои настройки.
 *
 * Записи, термы и мета-поля остаются: каталог не должен исчезать вместе с плагином,
 * а после переустановки данные снова окажутся на месте. Счётчики отправок формы
 * живут транзиентами с часовым сроком и уходят сами — перебирать таблицу опций
 * запросом мы не будем: SQL в проекте не пишем (локально база SQLite).
 *
 * @package designstack-core
 */

defined( 'WP_UNINSTALL_PLUGIN' ) || exit;

delete_option( 'designstack_core_version' );
